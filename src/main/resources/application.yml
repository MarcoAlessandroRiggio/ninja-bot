spring:
  main:
    log-startup-info: false
    banner-mode: off
  datasource:
    url: jdbc:sqlite:/Users/marcoriggio/develop/ninjabot/src/main/resources/dnd.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
logging:
  level:
    root: off
    org.pubcoding: info
  pattern:
    console: "%msg%n"